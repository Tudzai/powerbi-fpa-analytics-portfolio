Interaction QA note:

The final report layout uses static native filter chips instead of live slicers to keep the Desktop-rendered PBIX stable after automated package patching. The underlying model includes Year, Region, Business Unit, Scope, and Scenario fields, so live slicers can be added manually from Data/Model view if an interactive version is needed.

Open-check passed for `output/dashboard_final.pbix` on Power BI Desktop.
