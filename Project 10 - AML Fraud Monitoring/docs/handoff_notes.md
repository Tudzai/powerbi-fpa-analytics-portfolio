# Handoff Notes

Final native Power BI target: `output/dashboard_final.pbix`
Final interactive preview: `output/dashboard_final.html`
Final repaired PBIP target: `output/dashboard_final.pbip`

Data: deterministic synthetic AML/fraud monitoring data, seed `20260611`.
Tabs: Fraud & Compliance Overview; Alert & Customer Risk Deep Dive; Case SLA & Rule Governance.
Key KPIs: total transactions, flagged amount, alerts, suspicious cases, false positive rate, alert rate, alert-to-case conversion, SAR conversion, overdue cases, SLA compliance, rule precision.

## PBIX Build Status

Complete. The final PBIX is:

`C:\Users\Win\OneDrive\Codex\Portfolio\BI\Project 10 - AML Fraud Monitoring\output\dashboard_final.pbix`

The PBIX was rebuilt from a clean Power BI Desktop authoring session. The AML semantic model was pushed into the clean session, saved into `output/dashboard_model_clean.pbix`, verified offline with `pbi-tools export-data`, then promoted into `output/dashboard_final.pbix` with a legacy-compatible `Report/Layout`.

After the initial valid PBIX looked too plain, the report received an agent-led aesthetic refresh. The final visual system now uses a dark AML command-center shell, light KPI tiles, semantic red/amber/teal/violet accents, compact headers, dark chart panels, and updated design tokens in `build/config/theme.json`.

The original final PBIX failed because the report canvas was patched with PBIR/new-layout visual JSON. PBIX files built in the legacy Desktop layout stream require legacy visual containers where `config` is serialized as JSON text. The repaired layout is generated by `build/scripts/12_generate_legacy_report_layout.py` and applied by `build/scripts/13_apply_legacy_pbix_report.ps1`.

## Final Validation

- Power BI Desktop opened the exact final PBIX successfully.
- `PowerBIPackager.Validate` passed.
- `pbi-tools export-data` returned the expected AML tables and row counts.
- `pbi-tools extract` succeeded from the final PBIX.
- Screenshot evidence: `qa/screenshots/dashboard_final_pbix_dark_aesthetic_final.png`.

## Supplemental Outputs

- `output/dashboard_final.html` is the completed interactive dashboard preview.
- `output/dashboard_final.pbip` is a corrected shortcut to the final Power BI project.
- `output/dashboard_final_project/AML_Fraud_Monitoring_Command_Center.pbip` contains the repaired Power BI project package.
- `output/dashboard_final_project/AML_Fraud_Monitoring_Command_Center.SemanticModel/model.bim` contains the AML semantic model definition.
- `docs/aesthetic_redesign_brief.md` documents the final visual direction.
